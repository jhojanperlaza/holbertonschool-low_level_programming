#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include "main.h"
/**
* _abs - is a fuction to prints that computes the absolute value of an integer.
*@n: letter being tested
* Return: Always 0 (Success)
*/
int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	else
	{
	return (-n);
	}

	return (0);
}
