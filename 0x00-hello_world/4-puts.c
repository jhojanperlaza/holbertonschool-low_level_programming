#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char nombre[] = {"\"Programming is like building a multilingual puzzle\n"};

	puts(nombre);
	return (0);
}
